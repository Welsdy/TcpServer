# TcpServer
a simple tcpserver 
## Compile
Compile code with CMAKE  
1. Create a `build` directory and enter the `build` directory
```
mkdir build && cd build
cmake ../  
```
2. run the `make`  
```
make  
```
## Introduction




